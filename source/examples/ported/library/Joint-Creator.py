"""Port stub for AlibreScript ``Woodworking/Joint Creator.py``.

Same source as the help-v28 ``Joint-Creator.py`` script. See
``../Joint-Creator.py`` for the full porting plan (depends on
AlibreScript-only ``Sketch.StartFaceMapping`` /
``AssemblyPointtoPartPoint`` which AlibreX does not expose).
"""
from __future__ import annotations

def main() -> None:
    raise NotImplementedError(
        "Joint-Creator is a stub. See ../Joint-Creator.py for the porting plan."
    )

if __name__ == "__main__":
    main()
